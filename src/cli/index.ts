// Alpha Bros enterprise layer — CLI entry point (ab-enterprise bin).
// Deliberately a no-op in the v0.1 scaffold (Task 1); later tasks add the
// SCIM provisioning / migration commands here. tsup injects the
// `#!/usr/bin/env node` banner for this entry only (see tsup.config.ts).
export {};
